export default class connectionManager {
    
}

class connectionDetails {
    constructor(readonly niceName:string, readonly host:string, readonly port:number, readonly username:string, readonly password:string, readonly allowGlobalReuse:boolean) {}
}
